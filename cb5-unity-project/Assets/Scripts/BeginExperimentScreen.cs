using UnityEngine;
using System.Collections;
using System.IO;
using System.Text;
using System;

public class BeginExperimentScreen : MonoBehaviour {
	/*
	public tk2dUITextInput txtParticipantID;

	public Transform ConditionsPanel;
	public tk2dUIDropDownMenu ddlMode;
    
    // Use this for initialization
	void Start () {
		txtParticipantID.Text = Game.ParticipantID + string.Empty;

		if (Game.ConditionCount == 0 || Game.IsRandomAssignment) {
			ConditionsPanel.gameObject.SetActive(false);		
		}
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void OnBackClicked()
	{
		Application.LoadLevel ("ControlPanel");
	}

	void OnSubmitClicked()
	{
		//Game.CurrentCondition =int.Parse( ddlMode.SelectedItem.Substring(ddlMode.SelectedItem.Length- 1));
		//Game.ParticipantID = txtParticipantID.Text;

		Game.PrefsToFile ();
		Application.Quit ();
	}
	*/
}
